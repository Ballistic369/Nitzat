pluginManagement {
    repositories {
        google()          // Required for Android Gradle Plugin
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "NizatSearchApp"
include(":app")
